import React from "react";

class MainForm extends React.Component {
    render () {
        return (
        <form action="" method="post">
        {/* Este es el componente del main form principal
        Está en blanco. En él se incluirán 3 componentes más pequeños, los fieldset del formulario, que a su vez contiene dos componentes mas pequeños, el legend y su contenido */}
            
          </form>
        );
    }
}

export default MainForm;